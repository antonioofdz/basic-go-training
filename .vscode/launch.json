{
    "version": "0.2.0",
    "configurations": [
      {
        "name": "Launch test function",
        "type": "go",
        "cwd": "${workspaceFolder}",
        "request": "launch",
        "mode": "auto",
        "program": "${workspaceFolder}/main.go",
        "env": {
          "CONFIG_FILE": "config/config.yml"
        }
      }
    ]
  }